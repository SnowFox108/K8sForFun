apiVersion: v1
kind: Service
metadata:
  name: functiondurable
  labels:
    app: functiondurable
spec:
  selector:
    app: functiondurable
  ports:
    - protocol: TCP  
      port: 30597
      targetPort: 80
      name: http
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: functiondurable
  labels:
    app: functiondurable
spec:
  replicas: 1
  selector:
    matchLabels:
      app: functiondurable
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: functiondurable
      # annotations:
      #   dapr.io/enabled: "true"
      #   dapr.io/app-id: "moneycorp-options-cloud-supports-function"
      #   dapr.io/app-port: "32639"
      #   dapr.io/config: "appconfig"
    spec:
      containers:
        - name: functiondurable
          image: functiondurable:latest
          imagePullPolicy: Never
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: WEBSITE_HOSTNAME
              value: "localhost:30597"
            - name: AzureWebJobsStorage
              value: "UseDevelopmentStorage=true"
            - name: FUNCTIONS_WORKER_RUNTIME
              value: "dotnet-isolated"
            # - name: DAPR_HTTP_PORT
            #   value: "3500"
            # - name: PODNAME
            #   value: SUP01                    
          ports:
            - containerPort: 8080
              # protocol: TCP
          # volumeMounts:
          #   - mountPath: /kerberos-sidecar
          #     name: sidecar-volume
          #     readOnly: true
      restartPolicy: Always
      # volumes:
      #   - name: sidecar-volume
      #     persistentVolumeClaim:
      #       claimName: sidecar-volume
      #       readOnly: true
